{
  "name": "cucumber-express",
  "version": "0.0.0",
  "description": "Cucumber-js helper for validating response objects via Gherkin data tables",
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "example:assert": "npm start --prefix examples/assert",
    "example:build": "npm start --prefix examples/build",
    "example:schema": "npm start --prefix examples/schema",
    "example:full-flow": "npm start --prefix examples/full-flow",
    "example:errors": "npm start --prefix examples/errors"
  },
  "peerDependencies": {
    "@cucumber/cucumber": ">=9.0.0",
    "@faker-js/faker": ">=8.0.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.0.0",
    "@faker-js/faker": "^9.0.0",
    "@types/node": "^25.9.1",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0",
    "vitest": "^3.0.0"
  },
  "license": "MIT"
}
